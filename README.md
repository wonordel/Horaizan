# Horaizan Browser

# Языки/Languages
[Русский](README.md)

[English](README.en.md)

Horaizan - это open source браузер на PySide6 (Chromium/WebEngine). Проект включает кастомный интерфейс, настройки, инкогнито-режим, горячие клавиши и инструменты сборки под Linux/Windows 🌐.

## Особенности

- **Обновлённый UI**: Верхняя панель и вкладки переработаны (Firefox-inspired стиль, компактные кнопки, удобная адресная строка).
- **Вкладки с иконкой и названием сайта**: Показываются `favicon`, заголовок страницы, поддерживается перенос и закрытие вкладок.
- **Настройки внутри браузера**: Страница `horaizan://settings` с разделами:
  - Общие (тема, поисковик, поведение новой вкладки)
  - Приватность (очистка кеша/cookies, подтверждение очистки, запуск инкогнито)
  - Горячие клавиши (редактирование сочетаний)
- **Темы**: `dark`, `light`, `system` (подстройка под тему ОС).
- **Инкогнито-режим**: Отдельное окно с непостоянными cookies/кешем (по умолчанию горячая клавиша `Ctrl+Shift+N`).
- **Горячие клавиши**: Поддерживаются стандартные сочетания (`Ctrl+T`, `Ctrl+W`, `Ctrl+R`, `Ctrl+L`, `Ctrl+,`, `Alt+Left`, `Alt+Right`) с возможностью изменить их в настройках.
- **Иконка приложения**: Используется `app/themes/icon.png` для окна и сборок.

## Установка

1. Убедитесь, что у вас установлен Python 3.9 или выше 🐍.
2. Клонируйте репозиторий:

   ```bash
   git clone --depth 1 https://github.com/wonordel/Horaizan.git
   cd horaizan
   ```

3. Установите необходимые зависимости:

   ```bash
   pip install -r requirements.txt
   ```

## Запуск

Для запуска браузера выполните следующую команду (Linux 🐧):

```bash
python3 -m app.main
```
(если выдаёт ошибку, несколько раз перезапустите)

Для запуска браузера на Windows 🪟:

```batch
python -m app.main
```

## Использование

- **Домашняя страница**: При запуске браузера открывается домашняя страница выбранного поисковика.
- **Новая вкладка**: Нажмите кнопку "+" рядом с вкладками, чтобы открыть новую вкладку.
- **Навигация**: Используйте кнопки "Назад", "Вперёд" и "Перезагрузить" для навигации по страницам.
- **Адресная строка**: Введите URL или поисковый запрос в адресной строке и нажмите Enter.
- **Настройки**: Кнопка `☰` открывает внутреннюю страницу настроек.
- **Инкогнито**: Открывается из настроек или по `Ctrl+Shift+N`.
- **Системная тема**: При выборе `system` браузер автоматически переключается между светлой и тёмной темой ОС.

## Сборка и автоматизация (`build.sh`)

В репозитории есть сценарий сборки:

```bash
./build.sh
```

Доступные пункты:

- `1` - сборка `AppImage` (portable one-file)
- `2` - сборка `.exe` (на Windows) + попытка создать NSIS-инсталлятор
- `5` - подготовка/сборка пакета для `AUR`
- `4` - переустановка системных и Python-зависимостей

Особенности сборки AppImage:

- Скрипт автоматически проверяет `appimagetool`; если не найден, пытается установить `appimagetool-bin` через `yay` или `paru`.
- Иконка AppImage вшивается из `app/themes/icon.png`.
- Для совместимости используется сжатие `zstd`.
- Если в системе проблемы с FUSE, можно запускать так:

```bash
./dist/Horaizan-x86_64.AppImage --appimage-extract-and-run
```

## Структура проекта

```
.
├── app
│   ├── bridge.py
│   ├── __init__.py
│   ├── main.py
│   ├── profile.py
│   ├── __pycache__
│   │   ├── __init__.cpython-313.pyc
│   │   ├── main.cpython-313.pyc
│   │   ├── profile.cpython-313.pyc
│   │   ├── settings.cpython-313.pyc
│   │   ├── webview.cpython-313.pyc
│   │   └── window.cpython-313.pyc
│   ├── settings.py
│   ├── themes
│   │   ├── dark.qss
│   │   └── light.qss
│   ├── ui
│   │   ├── __init__.py
│   │   ├── menu.py
│   │   ├── __pycache__
│   │   │   ├── __init__.cpython-313.pyc
│   │   │   ├── menu.cpython-313.pyc
│   │   │   ├── settings_page.cpython-313.pyc
│   │   │   └── toolbar.cpython-313.pyc
│   │   ├── settings_html.py
│   │   └── toolbar.py
│   ├── webpage.py
│   ├── webview.py
│   └── window.py
├── build.sh
├── README.en.md
├── README.md
├── requirements.txt
└── requirements-build.txt
```

- **requirements.txt**: Основные runtime-зависимости.
- **requirements-build.txt**: Python-зависимости для сборки.
- **build.sh**: Интерактивный скрипт сборки и установки зависимостей.

## Авторы

- 1. Создатель и основной разработчик: **Xarays** (который забил на этот проект) - Discord: `xarays.gg`
- 2. Второстепенный разработчик: **Wonordel** (который продолжил делать проект) - Discord: `egorchik6767`
- 3. Третий разработчик: **Kidknightik** (который полностью переделал проект) - Discord: `kidknightik`

## Благодарности

Спасибо сообществу PySide6, ChatGPT, DeepSeek и всем, кто внёс вклад в развитие этого проекта ❤️.

---

**Примечание**: Этот проект находится в стадии неактивной разработки и могут появляться различные баги(они тут будут, сам сделал). Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, создавайте issue или отправляйте pull request ❤️.
